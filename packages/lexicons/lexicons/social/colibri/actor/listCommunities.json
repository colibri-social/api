{
	"lexicon": 1,
	"id": "social.colibri.actor.listCommunities",
	"defs": {
		"main": {
			"type": "query",
			"description": "Gets the requesting user's communities, in their preferred order.",
			"output": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["communities"],
					"properties": {
						"communities": {
							"type": "array",
							"description": "The requesting user's communities, in preferred sidebar order.",
							"items": {
								"type": "ref",
								"ref": "social.colibri.community.defs#communityView"
							}
						}
					}
				}
			},
			"errors": [
				{
					"name": "AuthRequired",
					"description": "The request has no valid service auth."
				}
			]
		}
	}
}
