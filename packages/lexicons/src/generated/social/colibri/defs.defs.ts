/*
 * THIS FILE WAS GENERATED BY "@atproto/lex". DO NOT EDIT.
 */

import { l } from "@atproto/lex";

const $nsid = "social.colibri.defs";

type $nsid = typeof $nsid;

export { $nsid };

/** A reference to a record in the same space. Record keys are unique per repo rather than per space, so a bare key cannot address a record in a space that aggregates many authors. */
type RecordRef = { $type?: "social.colibri.defs#recordRef";

  /**
   * DID of the repo holding the referenced record.
   */
  "did":l.DidString;

  /**
   * Record key of the referenced record.
   */
  "rkey":l.RecordKeyString };

export type { RecordRef };

/** A reference to a record in the same space. Record keys are unique per repo rather than per space, so a bare key cannot address a record in a space that aggregates many authors. */
const recordRef = /*#__PURE__*/ l.typedObject<RecordRef>($nsid, "recordRef", /*#__PURE__*/ l.object({"did":/*#__PURE__*/ l.string({"format":"did"}),"rkey":/*#__PURE__*/ l.string({"format":"record-key"})}));

export { recordRef };
