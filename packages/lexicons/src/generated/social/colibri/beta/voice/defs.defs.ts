/*
 * THIS FILE WAS GENERATED BY "@atproto/lex". DO NOT EDIT.
 */

import { l } from "@atproto/lex";

const $nsid = "social.colibri.beta.voice.defs";

type $nsid = typeof $nsid;

export { $nsid };

/** The RTP capabilities a WebRTC endpoint supports. */
type RtpCapabilities = { $type?: "social.colibri.beta.voice.defs#rtpCapabilities";

  /**
   * The opaque mediasoup capability blob.
   */
  "payload"?:l.LexMap };

export type { RtpCapabilities };

/** The RTP capabilities a WebRTC endpoint supports. */
const rtpCapabilities = /*#__PURE__*/ l.typedObject<RtpCapabilities>($nsid, "rtpCapabilities", /*#__PURE__*/ l.object({"payload":/*#__PURE__*/ l.optional(/*#__PURE__*/ l.lexMap())}));

export { rtpCapabilities };

/** Parameters for creating one side of a WebRTC transport. */
type TransportOptions = { $type?: "social.colibri.beta.voice.defs#transportOptions";

  /**
   * The transport's identifier.
   */
  "id":string;

  /**
   * The opaque mediasoup ICE parameters blob.
   */
  "iceParameters":l.LexMap;

  /**
   * The mediasoup ICE candidates, one opaque blob per candidate. This is an array, not a single object, because that is what a WebRTC transport hands back.
   */
  "iceCandidates":(l.LexMap)[];

  /**
   * The opaque mediasoup DTLS parameters blob.
   */
  "dtlsParameters":l.LexMap;

  /**
   * Which side of the call this transport carries.
   */
  "direction"?:"send" | "recv" | l.UnknownString };

export type { TransportOptions };

/** Parameters for creating one side of a WebRTC transport. */
const transportOptions = /*#__PURE__*/ l.typedObject<TransportOptions>($nsid, "transportOptions", /*#__PURE__*/ l.object({"id":/*#__PURE__*/ l.string(),"iceParameters":/*#__PURE__*/ l.lexMap(),"iceCandidates":/*#__PURE__*/ l.array(/*#__PURE__*/ l.lexMap(), ),"dtlsParameters":/*#__PURE__*/ l.lexMap(),"direction":/*#__PURE__*/ l.optional(/*#__PURE__*/ l.string<{"knownValues":["send","recv"]}>())}));

export { transportOptions };

/** A media producer available to consume. */
type ProducerInfo = { $type?: "social.colibri.beta.voice.defs#producerInfo";

  /**
   * The producer's identifier.
   */
  "producerId":string;

  /**
   * Who is producing this media.
   */
  "did":l.DidString;

  /**
   * The media kind.
   */
  "kind":"audio" | "video" | l.UnknownString;

  /**
   * Whether the producer is currently paused.
   */
  "paused"?:boolean;

  /**
   * What the producer captures.
   */
  "source"?:"microphone" | "camera" | "screen" | l.UnknownString };

export type { ProducerInfo };

/** A media producer available to consume. */
const producerInfo = /*#__PURE__*/ l.typedObject<ProducerInfo>($nsid, "producerInfo", /*#__PURE__*/ l.object({"producerId":/*#__PURE__*/ l.string(),"did":/*#__PURE__*/ l.string({"format":"did"}),"kind":/*#__PURE__*/ l.string<{"knownValues":["audio","video"]}>(),"paused":/*#__PURE__*/ l.optional(/*#__PURE__*/ l.boolean()),"source":/*#__PURE__*/ l.optional(/*#__PURE__*/ l.string<{"knownValues":["microphone","camera","screen"]}>())}));

export { producerInfo };

/** Parameters for consuming one producer. */
type ConsumerOptions = { $type?: "social.colibri.beta.voice.defs#consumerOptions";

  /**
   * The consumer's identifier.
   */
  "id":string;

  /**
   * The producer being consumed.
   */
  "producerId":string;

  /**
   * The media kind.
   */
  "kind":"audio" | "video" | l.UnknownString;

  /**
   * The opaque mediasoup RTP parameters blob.
   */
  "rtpParameters":l.LexMap };

export type { ConsumerOptions };

/** Parameters for consuming one producer. */
const consumerOptions = /*#__PURE__*/ l.typedObject<ConsumerOptions>($nsid, "consumerOptions", /*#__PURE__*/ l.object({"id":/*#__PURE__*/ l.string(),"producerId":/*#__PURE__*/ l.string(),"kind":/*#__PURE__*/ l.string<{"knownValues":["audio","video"]}>(),"rtpParameters":/*#__PURE__*/ l.lexMap()}));

export { consumerOptions };

/** A change in whether someone is currently speaking. */
type SpeakingUpdate = { $type?: "social.colibri.beta.voice.defs#speakingUpdate";

  /**
   * Who this update is about.
   */
  "did":l.DidString;

  /**
   * Whether they are currently speaking.
   */
  "speaking":boolean;

  /**
   * An audio level in dBFS.
   */
  "level"?:number };

export type { SpeakingUpdate };

/** A change in whether someone is currently speaking. */
const speakingUpdate = /*#__PURE__*/ l.typedObject<SpeakingUpdate>($nsid, "speakingUpdate", /*#__PURE__*/ l.object({"did":/*#__PURE__*/ l.string({"format":"did"}),"speaking":/*#__PURE__*/ l.boolean(),"level":/*#__PURE__*/ l.optional(/*#__PURE__*/ l.integer())}));

export { speakingUpdate };

/** Joins a voice channel. A connection must join before sending any other frame, and joining a second channel leaves the first. */
type Join = { $type?: "social.colibri.beta.voice.defs#join";

  /**
   * The voice channel to join.
   */
  "channel":l.SpaceRefString };

export type { Join };

/** Joins a voice channel. A connection must join before sending any other frame, and joining a second channel leaves the first. */
const join = /*#__PURE__*/ l.typedObject<Join>($nsid, "join", /*#__PURE__*/ l.object({"channel":/*#__PURE__*/ l.string({"format":"space-ref"})}));

export { join };

/** Leaves the voice channel this connection is joined to. */
type Leave = { $type?: "social.colibri.beta.voice.defs#leave" };

export type { Leave };

/** Leaves the voice channel this connection is joined to. */
const leave = /*#__PURE__*/ l.typedObject<Leave>($nsid, "leave", /*#__PURE__*/ l.object({}));

export { leave };

/** Requests the router's RTP capabilities for the joined channel. */
type GetRtpCapabilities = { $type?: "social.colibri.beta.voice.defs#getRtpCapabilities" };

export type { GetRtpCapabilities };

/** Requests the router's RTP capabilities for the joined channel. */
const getRtpCapabilities = /*#__PURE__*/ l.typedObject<GetRtpCapabilities>($nsid, "getRtpCapabilities", /*#__PURE__*/ l.object({}));

export { getRtpCapabilities };

/** Requests a new WebRTC transport for the joined channel. */
type CreateTransport = { $type?: "social.colibri.beta.voice.defs#createTransport";

  /**
   * Whether this transport will send or receive media.
   */
  "direction":"send" | "recv" | l.UnknownString };

export type { CreateTransport };

/** Requests a new WebRTC transport for the joined channel. */
const createTransport = /*#__PURE__*/ l.typedObject<CreateTransport>($nsid, "createTransport", /*#__PURE__*/ l.object({"direction":/*#__PURE__*/ l.string<{"knownValues":["send","recv"]}>()}));

export { createTransport };

/** Completes DTLS negotiation for a transport this connection created. */
type ConnectTransport = { $type?: "social.colibri.beta.voice.defs#connectTransport";

  /**
   * The transport to connect.
   */
  "transportId":string;

  /**
   * The opaque mediasoup DTLS parameters blob.
   */
  "dtlsParameters":l.LexMap };

export type { ConnectTransport };

/** Completes DTLS negotiation for a transport this connection created. */
const connectTransport = /*#__PURE__*/ l.typedObject<ConnectTransport>($nsid, "connectTransport", /*#__PURE__*/ l.object({"transportId":/*#__PURE__*/ l.string(),"dtlsParameters":/*#__PURE__*/ l.lexMap()}));

export { connectTransport };

/** Starts producing media on a send transport this connection created. */
type Produce = { $type?: "social.colibri.beta.voice.defs#produce";

  /**
   * The send transport to produce on.
   */
  "transportId":string;

  /**
   * The media kind.
   */
  "kind":"audio" | "video" | l.UnknownString;

  /**
   * The opaque mediasoup RTP parameters blob.
   */
  "rtpParameters":l.LexMap;

  /**
   * What this producer captures.
   */
  "source":"microphone" | "camera" | "screen" | l.UnknownString };

export type { Produce };

/** Starts producing media on a send transport this connection created. */
const produce = /*#__PURE__*/ l.typedObject<Produce>($nsid, "produce", /*#__PURE__*/ l.object({"transportId":/*#__PURE__*/ l.string(),"kind":/*#__PURE__*/ l.string<{"knownValues":["audio","video"]}>(),"rtpParameters":/*#__PURE__*/ l.lexMap(),"source":/*#__PURE__*/ l.string<{"knownValues":["microphone","camera","screen"]}>()}));

export { produce };

/** Closes a producer this connection owns. */
type CloseProducer = { $type?: "social.colibri.beta.voice.defs#closeProducer";

  /**
   * The producer to close.
   */
  "producerId":string };

export type { CloseProducer };

/** Closes a producer this connection owns. */
const closeProducer = /*#__PURE__*/ l.typedObject<CloseProducer>($nsid, "closeProducer", /*#__PURE__*/ l.object({"producerId":/*#__PURE__*/ l.string()}));

export { closeProducer };

/** Starts consuming another peer's producer on a receive transport this connection created. */
type Consume = { $type?: "social.colibri.beta.voice.defs#consume";

  /**
   * The receive transport to consume on.
   */
  "transportId":string;

  /**
   * The producer to consume.
   */
  "producerId":string;

  /**
   * This connection's opaque mediasoup RTP capabilities blob.
   */
  "rtpCapabilities":l.LexMap };

export type { Consume };

/** Starts consuming another peer's producer on a receive transport this connection created. */
const consume = /*#__PURE__*/ l.typedObject<Consume>($nsid, "consume", /*#__PURE__*/ l.object({"transportId":/*#__PURE__*/ l.string(),"producerId":/*#__PURE__*/ l.string(),"rtpCapabilities":/*#__PURE__*/ l.lexMap()}));

export { consume };

/** Resumes a consumer this connection created, which is created paused by default. */
type ResumeConsumer = { $type?: "social.colibri.beta.voice.defs#resumeConsumer";

  /**
   * The consumer to resume.
   */
  "consumerId":string };

export type { ResumeConsumer };

/** Resumes a consumer this connection created, which is created paused by default. */
const resumeConsumer = /*#__PURE__*/ l.typedObject<ResumeConsumer>($nsid, "resumeConsumer", /*#__PURE__*/ l.object({"consumerId":/*#__PURE__*/ l.string()}));

export { resumeConsumer };

/** Updates this connection's own mute or deafen state. An absent field leaves the current value unchanged. */
type SetSelfState = { $type?: "social.colibri.beta.voice.defs#setSelfState";

  /**
   * Whether the microphone is muted.
   */
  "muted"?:boolean;

  /**
   * Whether incoming audio is silenced.
   */
  "deafened"?:boolean };

export type { SetSelfState };

/** Updates this connection's own mute or deafen state. An absent field leaves the current value unchanged. */
const setSelfState = /*#__PURE__*/ l.typedObject<SetSelfState>($nsid, "setSelfState", /*#__PURE__*/ l.object({"muted":/*#__PURE__*/ l.optional(/*#__PURE__*/ l.boolean()),"deafened":/*#__PURE__*/ l.optional(/*#__PURE__*/ l.boolean())}));

export { setSelfState };

/** Keeps the connection alive. The server answers with an ack. */
type Heartbeat = { $type?: "social.colibri.beta.voice.defs#heartbeat" };

export type { Heartbeat };

/** Keeps the connection alive. The server answers with an ack. */
const heartbeat = /*#__PURE__*/ l.typedObject<Heartbeat>($nsid, "heartbeat", /*#__PURE__*/ l.object({}));

export { heartbeat };

/** Answers a heartbeat. */
type Ack = { $type?: "social.colibri.beta.voice.defs#ack" };

export type { Ack };

/** Answers a heartbeat. */
const ack = /*#__PURE__*/ l.typedObject<Ack>($nsid, "ack", /*#__PURE__*/ l.object({}));

export { ack };

/** Something the client sent could not be acted on. */
type Error = { $type?: "social.colibri.beta.voice.defs#error";

  /**
   * Error name.
   */
  "error":"InvalidFrame" | "NotJoined" | "ChannelNotFound" | "NotVoiceChannel" | "Forbidden" | "NotFound" | l.UnknownString;

  /**
   * Human-readable detail.
   */
  "message"?:string };

export type { Error };

/** Something the client sent could not be acted on. */
const error = /*#__PURE__*/ l.typedObject<Error>($nsid, "error", /*#__PURE__*/ l.object({"error":/*#__PURE__*/ l.string<{"knownValues":["InvalidFrame","NotJoined","ChannelNotFound","NotVoiceChannel","Forbidden","NotFound"]}>(),"message":/*#__PURE__*/ l.optional(/*#__PURE__*/ l.string())}));

export { error };

/** Confirms this connection has joined a voice channel. */
type Joined = { $type?: "social.colibri.beta.voice.defs#joined";

  /**
   * The voice channel now joined.
   */
  "channel":l.SpaceRefString };

export type { Joined };

/** Confirms this connection has joined a voice channel. */
const joined = /*#__PURE__*/ l.typedObject<Joined>($nsid, "joined", /*#__PURE__*/ l.object({"channel":/*#__PURE__*/ l.string({"format":"space-ref"})}));

export { joined };

/** Another peer joined the voice channel this connection is in. */
type PeerJoined = { $type?: "social.colibri.beta.voice.defs#peerJoined";

  /**
   * Who joined.
   */
  "did":l.DidString };

export type { PeerJoined };

/** Another peer joined the voice channel this connection is in. */
const peerJoined = /*#__PURE__*/ l.typedObject<PeerJoined>($nsid, "peerJoined", /*#__PURE__*/ l.object({"did":/*#__PURE__*/ l.string({"format":"did"})}));

export { peerJoined };

/** A peer left the voice channel this connection is in. */
type PeerLeft = { $type?: "social.colibri.beta.voice.defs#peerLeft";

  /**
   * Who left.
   */
  "did":l.DidString };

export type { PeerLeft };

/** A peer left the voice channel this connection is in. */
const peerLeft = /*#__PURE__*/ l.typedObject<PeerLeft>($nsid, "peerLeft", /*#__PURE__*/ l.object({"did":/*#__PURE__*/ l.string({"format":"did"})}));

export { peerLeft };

/** A peer's producer closed. */
type ProducerRemoved = { $type?: "social.colibri.beta.voice.defs#producerRemoved";

  /**
   * The producer that closed.
   */
  "producerId":string;

  /**
   * Whose producer this was.
   */
  "did":l.DidString };

export type { ProducerRemoved };

/** A peer's producer closed. */
const producerRemoved = /*#__PURE__*/ l.typedObject<ProducerRemoved>($nsid, "producerRemoved", /*#__PURE__*/ l.object({"producerId":/*#__PURE__*/ l.string(),"did":/*#__PURE__*/ l.string({"format":"did"})}));

export { producerRemoved };

/** The server removed this client from the voice channel. Sent only to the client it applies to. */
type Disconnected = { $type?: "social.colibri.beta.voice.defs#disconnected";

  /**
   * Why it happened.
   */
  "reason"?:"moderator" | "superseded" | "channelGone" | l.UnknownString };

export type { Disconnected };

/** The server removed this client from the voice channel. Sent only to the client it applies to. */
const disconnected = /*#__PURE__*/ l.typedObject<Disconnected>($nsid, "disconnected", /*#__PURE__*/ l.object({"reason":/*#__PURE__*/ l.optional(/*#__PURE__*/ l.string<{"knownValues":["moderator","superseded","channelGone"]}>())}));

export { disconnected };

/** A peer's mute or deafen state changed, whether they did it themselves or a moderator did it to them. */
type ModerationChanged = { $type?: "social.colibri.beta.voice.defs#moderationChanged";

  /**
   * Whose state changed.
   */
  "did":l.DidString;

  /**
   * Whether the microphone is muted, for any reason.
   */
  "muted":boolean;

  /**
   * Whether incoming audio is silenced, for any reason.
   */
  "deafened":boolean;

  /**
   * Whether a moderator muted them. When this is true the peer cannot unmute themselves.
   */
  "serverMuted"?:boolean;

  /**
   * Whether a moderator deafened them. When this is true the peer cannot undeafen themselves.
   */
  "serverDeafened"?:boolean };

export type { ModerationChanged };

/** A peer's mute or deafen state changed, whether they did it themselves or a moderator did it to them. */
const moderationChanged = /*#__PURE__*/ l.typedObject<ModerationChanged>($nsid, "moderationChanged", /*#__PURE__*/ l.object({"did":/*#__PURE__*/ l.string({"format":"did"}),"muted":/*#__PURE__*/ l.boolean(),"deafened":/*#__PURE__*/ l.boolean(),"serverMuted":/*#__PURE__*/ l.optional(/*#__PURE__*/ l.boolean()),"serverDeafened":/*#__PURE__*/ l.optional(/*#__PURE__*/ l.boolean())}));

export { moderationChanged };
