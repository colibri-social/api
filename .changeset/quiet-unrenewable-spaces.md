---
"@colibri-social/space-sync": patch
"@colibri-social/appview": patch
---

The sync engine can now ask the appview whether a space is renewable at all, and parks the ones that are not without warning on every startup
