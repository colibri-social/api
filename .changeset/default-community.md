---
"@colibri-social/appview": minor
"@colibri-social/lexicons": minor
---

Adds the optional `DEFAULT_COMMUNITY_DID` setting, which `describeServer` reports as `defaultCommunity` so clients can join new users to it
