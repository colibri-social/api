---
"@colibri-social/community": minor
---

Report progress while migrating a legacy community, find one from read cursors and message history when nothing else names it, and honor a role's per-channel overrides when checking `label.apply`
